name: ChiaWallet
rust:
  # Point to the Chia wallet SDK repo
  # repo: https://github.com/ronickg/chia-wallet-sdk
  # branch: main # or whichever branch/tag you want to use
  # manifestPath: uniffi/Cargo.toml # Path to the uniffi subfolder's Cargo.toml
  directory: ./chia-wallet-sdk/uniffi # Path to the uniffi subfolder
  manifest-path: Cargo.toml # Path to the uniffi subfolder's Cargo.toml
